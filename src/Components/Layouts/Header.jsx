import { Link } from "react-router-dom";

const Header = () => {
    return(
        <>
            <Link to="/" className="text-3xl font-bold underline">Logo App</Link>
        </>
    );
}

export default Header;